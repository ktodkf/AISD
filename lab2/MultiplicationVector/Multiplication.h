#pragma once
#include <vector>
#include <string>


using namespace std;

const string ENTERVECTOR = "Enter vector:";
const string INCORRECTDATA = "You entered incorrect data";

vector <float> ReatText();
float MinElement(vector<float>& vec_num);
vector <float> VectorMultiplication(vector<float>& vec_num);